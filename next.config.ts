import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
    productionBrowserSourceMaps: true, // important for debugging in browser
};

export default nextConfig;
